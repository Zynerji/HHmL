#!/usr/bin/env python3
"""
Setup script for HHmL package.

For modern installations, prefer using pyproject.toml with:
    pip install -e .

This setup.py is maintained for backwards compatibility.
"""

from setuptools import setup

# Configuration is in pyproject.toml
# This file exists for editable installs and backwards compatibility
if __name__ == "__main__":
    setup()
